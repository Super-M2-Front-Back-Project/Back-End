# Configuration Serveur
PORT=3000
NODE_ENV=development

# Supabase Configuration
# Récupérer ces valeurs depuis : https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://votre-projet-id.supabase.co
SUPABASE_ANON_KEY=votre-clé-publique-anon
SUPABASE_SERVICE_ROLE_KEY=votre-clé-service-role-secrète

# IMPORTANT : Ne JAMAIS commiter le fichier .env avec les vraies valeurs
# Copier ce fichier en .env et remplir avec vos vraies clés

# JWT (si vous utilisez JWT custom au lieu de Supabase Auth)
# JWT_SECRET=votre-secret-jwt-très-long-et-sécurisé
# JWT_EXPIRES_IN=7d

# CORS (Optionnel)
# CORS_ORIGIN=http://localhost:5173

# Rate Limiting (Optionnel)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
